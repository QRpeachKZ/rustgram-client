[package]
name = "rustgram-dialog-filter-invite-link"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

description = "Dialog filter invite link types for Telegram MTProto client"

[dependencies]
rustgram-types = { path = "../types" }
thiserror = "2.0"

[dev-dependencies]
serde_json = "1.0"

[lints]
rust = { unexpected-cfgs = "allow" }
clippy = { all = "deny", unwrap-used = "deny", expect-used = "deny" }

[package.metadata.docs.rs]
all-features = true
