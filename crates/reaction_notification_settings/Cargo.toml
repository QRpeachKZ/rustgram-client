[package]
name = "rustgram-reaction-notification-settings"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

description = "Reaction notification settings for Telegram MTProto client"

[dependencies]
rustgram-reaction-notifications-from = { path = "../reaction_notifications-from" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
proptest = { workspace = true }

[features]
default = []

[package.metadata.docs.rs]
all-features = true

[lints]
rust = { unexpected-cfgs = "allow" }
clippy = { all = "deny", unwrap-used = "deny", expect-used = "deny" }
