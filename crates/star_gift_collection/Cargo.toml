[package]
name = "rustgram-star-gift-collection"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

description = "Star gift collection types for Telegram MTProto client"

[dependencies]
rustgram-types = { path = "../types" }
thiserror = "2.0"

[dev-dependencies]
serde_json = "1.0"

[lints]
rust = { unexpected_cfgs = "allow" }
clippy = { all = "deny", unwrap_used = "deny", expect_used = "deny" }

[package.metadata.docs.rs]
all-features = true
