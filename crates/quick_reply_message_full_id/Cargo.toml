[package]
name = "rustgram-quick-reply-message-full-id"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

description = "Quick reply message full ID for Telegram MTProto client"

[dependencies]
rustgram-types = { path = "../types" }
rustgram-quick-reply-shortcut-id = { path = "../quick_reply_shortcut-id" }
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
proptest = { workspace = true }

[features]
default = []

[package.metadata.docs.rs]
all-features = true

[lints]
rust = { unexpected-cfgs = "allow" }
clippy = { all = "deny", unwrap-used = "deny", expect-used = "deny" }
