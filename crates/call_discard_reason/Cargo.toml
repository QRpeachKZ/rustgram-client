[package]
name = "rustgram-call-discard-reason"
version = "0.1.0"
edition = "2021"
authors = ["rustgram-client contributors"]
license = "MIT OR Apache-2.0"
description = "Call discard reason types for Telegram MTProto client"
repository = "https://github.com/yourusername/rustgram-client"
keywords = ["telegram", "mtproto", "calls", "voip"]
categories = ["data-structures"]

[dependencies]
serde = { version = "1.0", optional = true }

[features]
default = []
serde = ["dep:serde"]

[dev-dependencies]
