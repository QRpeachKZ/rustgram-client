[package]
name = "rustgram-story-forward-info"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

description = "Story forward information tracking for Telegram MTProto client"
keywords = ["telegram", "mtproto", "stories", "forward", "repost"]
categories = ["data-structures"]

[dependencies]
rustgram-types = { path = "../types" }
rustgram-active-story-state = { path = "../active_story-state" }
thiserror = { workspace = true }

[dev-dependencies]

[lib]
name = "rustgram_story_forward_info"
path = "src/lib.rs"

[features]
default = []
full = []
