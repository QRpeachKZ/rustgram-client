[package]
name = "rustgram-people-nearby"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

description = "People nearby functionality for Telegram MTProto client"

[dependencies]
# Error handling
thiserror = { workspace = true }

# Logging
tracing = { workspace = true }

# Internal crates
rustgram-types = { path = "../types" }
rustgram-venue = { path = "../venue" }

# Serialization (optional)
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde", "rustgram-venue/serde"]

[lints]
rust = { unexpected-cfgs = "allow" }
clippy = { all = "deny", unwrap-used = "deny", expect-used = "deny" }

[package.metadata.docs.rs]
all-features = true
